/// <reference types="react" />
import { ComponentMeta, ComponentStory } from "@storybook/react";
declare const _default: ComponentMeta<
	({
		open,
		anchorElement,
		onClose,
		children,
		classNamePaper,
		classNameBackdrop,
		anchorPosition,
		popoverPosition,
	}: import("./popover").PopoverProps) => JSX.Element
>;
export default _default;
export declare const Default: ComponentStory<
	({
		open,
		anchorElement,
		onClose,
		children,
		classNamePaper,
		classNameBackdrop,
		anchorPosition,
		popoverPosition,
	}: import("./popover").PopoverProps) => JSX.Element
>;
export declare const Custom: ComponentStory<
	({
		open,
		anchorElement,
		onClose,
		children,
		classNamePaper,
		classNameBackdrop,
		anchorPosition,
		popoverPosition,
	}: import("./popover").PopoverProps) => JSX.Element
>;
